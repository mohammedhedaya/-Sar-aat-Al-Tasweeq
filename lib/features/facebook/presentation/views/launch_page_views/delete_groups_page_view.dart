import 'package:flutter/material.dart';

class DeleteGroupsPageView extends StatelessWidget {
  const DeleteGroupsPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Delete Groups Page View",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
